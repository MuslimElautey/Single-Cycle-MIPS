# Single-Cycle-MIPS
Single-Cycle implementation of the MIPS processor in Verilog, supporting basic instruction types (R-type, I-type, J-type and memory operations). Designed and simulated to demonstrate fundamental CPU architecture concepts.
